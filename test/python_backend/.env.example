# FastAPI + MariaDB 연결 정보 (복사 후 .env 로 저장하고 값 채우기)
# 주의: .env 파일은 git 에 올리지 마세요.
# MariaDB Cloud (SkySQL) 사용 시 호스트/포트/SSL 설정 후 DB_SSL=true

DB_HOST=localhost
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=your_database
# DB_SSL=true   # 클라우드 DB 사용 시 SSL 적용# Node.js 알림 웹훅 (위험/이상 징후 시 Node가 Slack 전송·DB 기록)
NODE_ALERT_WEBHOOK_URL=http://localhost:3000/api/dashboard/alert# 이상 징후 자동 알림 규칙 (선택)
# DANGER_THRESHOLD=190
# ANOMALY_WINDOW=20
# ANOMALY_LOOKBACK=50
# ANOMALY_DEFECT_RATIO=0.3
# ANOMALY_CONSECUTIVE=3