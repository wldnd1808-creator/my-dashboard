# MariaDB Cloud (SkySQL) - Node.js용
# 이 파일을 .env 로 복사한 뒤 비밀번호·CA 경로를 채우세요.
# cp .env.mariadb-cloud.example .env

PORT=3000
DB_HOST=serverless-europe-west9.sysp0000.db2.skysql.com
DB_PORT=4078
DB_USER=DBPGF30741618
DB_PASSWORD=[여기에 본인의 비밀번호 입력]
DB_NAME=default

# 클라우드 환경: SSL 필수
DB_SSL=true

# CA 인증서 경로 (SkySQL 콘솔에서 다운로드한 .pem 파일)
# Windows 예: C:/path/to/skysql_chain.pem
# Linux/Mac 예: /path/to/skysql_chain.pem
DB_CA_PATH=

# FastAPI 예측 서버
PREDICTION_API_URL=http://localhost:8000
