# Node.js + MariaDB / FastAPI 연동 (복사 후 .env 로 저장하고 값 채우기)
# .env 는 git 에 올리지 마세요.

PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=your_database

# FastAPI 예측 서버 주소 (가이드 ⑤)
PREDICTION_API_URL=http://localhost:8000

# Slack Webhook URL (위험/이상 징후 알림 발송용, Incoming Webhooks 설정 후 URL 입력)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# 이상 징후 검사 주기(ms, 기본 5분). 0 이면 자동 검사 비활성화
# ANOMALY_CHECK_INTERVAL_MS=300000

# 설비 고장 확률 검사 (FastAPI /api/equipment/failure-probability)
# FAILURE_PROB_THRESHOLD=0.8
# FAILURE_PROB_CHECK_INTERVAL_MS=60000
