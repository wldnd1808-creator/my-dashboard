// 대시보드 설정 파일
// 이 파일에서 입력 필드의 이름과 범위를 변경할 수 있습니다.

const DASHBOARD_CONFIG = {
  // API 베이스 URL
  // - 빈 문자열 "": Node 백엔드와 같은 출처 (대시보드는 Node에서 서빙, 권장)
  // - "http://localhost:8000": FastAPI만 사용 시 (예측/훈련·예측 데이터 직접 호출)
  apiBase: "",

  // Feature1 설정 (min/max = 입력 허용 범위, 권장 범위는 750~1000°C)
  feature1: {
    label: "소성온도",
    unit: "°C",
    placeholder: "소성온도",
    min: 500,                    // 입력 허용 최소 (750 미만 입력 시 이상 징후 경고)
    max: 1200,                   // 입력 허용 최대 (1000 초과 입력 시 이상 징후 경고)
    default: 900,
    description: "소성온도 (Feature1, °C)"
  },
  
  // Feature2 설정 (min/max = 입력 허용 범위, 권장 범위는 8~24시간)
  feature2: {
    label: "소성시간",
    unit: "시간",
    placeholder: "소성시간",
    min: 0,                      // 입력 허용 최소 (8 미만 입력 시 이상 징후 경고)
    max: 48,                     // 입력 허용 최대 (24 초과 입력 시 이상 징후 경고)
    default: 15,
    description: "소성시간 (Feature2, 시간)"
  },
  
  // 힌트 메시지 (권장 범위 밖 입력 시 예측은 되고, 경고만 표시됨)
  hintMessage: "💡 권장 범위: 소성온도 750~1000°C, 소성시간 8~24시간 (범위 밖 입력 시 이상 징후 경고 표시)",
  
  // 차트 제목
  chartTitle: "소성온도 vs 방전용량 추이",
  chartDescription: "훈련 데이터에서 소성온도(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."
};

// 다른 예시 설정들:

// 예시 1: 가열온도/가열시간으로 변경
/*
const DASHBOARD_CONFIG = {
  feature1: {
    label: "가열온도",
    unit: "°C",
    placeholder: "가열온도",
    min: 750,
    max: 1000,
    default: 900,
    description: "가열온도 (Feature1, °C)"
  },
  feature2: {
    label: "가열시간",
    unit: "시간",
    placeholder: "가열시간",
    min: 8,
    max: 24,
    default: 15,
    description: "가열시간 (Feature2, 시간)"
  },
  hintMessage: "💡 입력 범위: 가열온도 750~1000°C, 가열시간 8~24시간",
  chartTitle: "가열온도 vs 방전용량 추이",
  chartDescription: "훈련 데이터에서 가열온도(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."
};
*/

// 예시 2: 처리온도/처리시간으로 변경
/*
const DASHBOARD_CONFIG = {
  feature1: {
    label: "처리온도",
    unit: "°C",
    placeholder: "처리온도",
    min: 750,
    max: 1000,
    default: 900,
    description: "처리온도 (Feature1, °C)"
  },
  feature2: {
    label: "처리시간",
    unit: "시간",
    placeholder: "처리시간",
    min: 8,
    max: 24,
    default: 15,
    description: "처리시간 (Feature2, 시간)"
  },
  hintMessage: "💡 입력 범위: 처리온도 750~1000°C, 처리시간 8~24시간",
  chartTitle: "처리온도 vs 방전용량 추이",
  chartDescription: "훈련 데이터에서 처리온도(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."
};
*/

// 예시 3: 완전히 다른 의미로 변경 (예: 압력/속도)
/*
const DASHBOARD_CONFIG = {
  feature1: {
    label: "압력",
    unit: "MPa",
    placeholder: "압력",
    min: 750,
    max: 1000,
    default: 900,
    description: "압력 (Feature1, MPa)"
  },
  feature2: {
    label: "속도",
    unit: "m/s",
    placeholder: "속도",
    min: 8,
    max: 24,
    default: 15,
    description: "속도 (Feature2, m/s)"
  },
  hintMessage: "💡 입력 범위: 압력 750~1000 MPa, 속도 8~24 m/s",
  chartTitle: "압력 vs 방전용량 추이",
  chartDescription: "훈련 데이터에서 압력(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."
};
*/
