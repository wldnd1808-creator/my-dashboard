# 레이블 변경 가이드

대시보드의 "온도", "시간" 레이블을 다른 이름으로 변경하는 방법입니다.

## 빠른 변경 방법

`dashboard/config.js` 파일을 열어서 수정하세요.

## 변경 예시

### 예시 1: 가열온도/가열시간으로 변경

`config.js` 파일에서 다음과 같이 수정:

```javascript
const DASHBOARD_CONFIG = {
  feature1: {
    label: "가열온도",           // 변경
    unit: "°C",
    placeholder: "가열온도",
    min: 750,
    max: 1000,
    default: 900,
    description: "가열온도 (Feature1, °C)"  // 변경
  },
  feature2: {
    label: "가열시간",           // 변경
    unit: "시간",
    placeholder: "가열시간",
    min: 8,
    max: 24,
    default: 15,
    description: "가열시간 (Feature2, 시간)"  // 변경
  },
  hintMessage: "💡 입력 범위: 가열온도 750~1000°C, 가열시간 8~24시간",  // 변경
  chartTitle: "가열온도 vs 방전용량 추이",  // 변경
  chartDescription: "훈련 데이터에서 가열온도(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."  // 변경
};
```

### 예시 2: 처리온도/처리시간으로 변경

```javascript
const DASHBOARD_CONFIG = {
  feature1: {
    label: "처리온도",
    unit: "°C",
    description: "처리온도 (Feature1, °C)"
  },
  feature2: {
    label: "처리시간",
    unit: "시간",
    description: "처리시간 (Feature2, 시간)"
  },
  hintMessage: "💡 입력 범위: 처리온도 750~1000°C, 처리시간 8~24시간",
  chartTitle: "처리온도 vs 방전용량 추이",
  chartDescription: "훈련 데이터에서 처리온도(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."
};
```

### 예시 3: 완전히 다른 의미로 변경 (예: 압력/속도)

```javascript
const DASHBOARD_CONFIG = {
  feature1: {
    label: "압력",
    unit: "MPa",
    min: 750,
    max: 1000,
    default: 900,
    description: "압력 (Feature1, MPa)"
  },
  feature2: {
    label: "속도",
    unit: "m/s",
    min: 8,
    max: 24,
    default: 15,
    description: "속도 (Feature2, m/s)"
  },
  hintMessage: "💡 입력 범위: 압력 750~1000 MPa, 속도 8~24 m/s",
  chartTitle: "압력 vs 방전용량 추이",
  chartDescription: "훈련 데이터에서 압력(Feature1)에 따른 방전용량(Target)의 변화를 보여줍니다."
};
```

## 변경할 수 있는 항목

1. **label**: 짧은 이름 (예: "가열온도")
2. **unit**: 단위 (예: "°C", "시간", "MPa")
3. **description**: 전체 설명 (예: "가열온도 (Feature1, °C)")
4. **min/max**: 입력 범위
5. **default**: 기본값
6. **hintMessage**: 힌트 메시지
7. **chartTitle**: 차트 제목
8. **chartDescription**: 차트 설명

## 변경 후 확인

1. `config.js` 파일 저장
2. 브라우저에서 대시보드 새로고침 (F5)
3. 변경된 레이블 확인

## 주의사항

- `label`, `unit`, `description`은 모두 변경하는 것을 권장합니다
- `min`, `max` 값은 실제 데이터 범위에 맞게 설정하세요
- 단위(`unit`)를 변경하면 힌트 메시지와 차트 설명도 함께 수정하세요
